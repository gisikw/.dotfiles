aliases:
  be: bundle exec
  berc: bundle exec rails c
  webs: python -m SimpleHTTPServer
  dot: (cd ~/.dotfiles && vim bashrc && up)
  m: mux
  z: fg
symlinks:
  .vimrc: .dotfiles/vimrc
  .tmux.conf: .dotfiles/tmux.conf
  .gitconfig: .dotfiles/gitconfig
