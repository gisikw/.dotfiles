# ~/.tmuxinator/revid.yml

name: revid
root: ~/Projects/revid
pre: yarn install
windows:
  - app:
    - vim -c 'NERDTreeToggle'
  - server:
    - filewatcher -rd 'src/server.js' 'sleep 1 && yarn server'
  - client:
    - yarn start
