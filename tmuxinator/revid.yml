# ~/.tmuxinator/revid.yml

name: revid
root: ~/Projects/revid
pre: yarn install
windows:
  - app:
    - vim -c 'NERDTreeToggle'
  - server:
    - filewatcher -rd 'src/server.js' 'yarn server'
  - client:
    - yarn start
